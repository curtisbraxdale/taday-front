import * as React from 'react';

// Placeholder chart component - not used in current implementation
export const Chart = ({ children }: { children: React.ReactNode }) => {
  return <div className="chart-container">{children}</div>;
};